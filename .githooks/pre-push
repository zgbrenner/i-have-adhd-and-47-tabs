#!/usr/bin/env bash
set -euo pipefail

ROOT="$(git rev-parse --show-toplevel)"
cd "$ROOT"

printf 'Running local repository checks before push...\n'
make check

git diff --exit-code -- dist/i-have-adhd-and-47-tabs.zip
